services:
  # job_market_db:
  #   container_name: job_market_database
  #   image: job_market_database
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_DB: JobMarketDB
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: test

  #   healthcheck:
  #     test: ["CMD", "pg_isready", "-U", "admin", "-d", "JobMarketDB"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #   networks:
  #     - inner_network

  job_market_db_master:
    image: job_market_database
    container_name: job_market_db_master
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: JobMarketDB
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: test
      POSTGRES_HOST_AUTH_METHOD: "scram-sha-256\nhost all all 0.0.0.0/0 md5"
      POSTGRES_INITDB_ARGS: "--auth-host=scram-sha-256"
    command:
      - "postgres"
      - "-c"
      - "config_file=/tmp/postgresql.conf"
      - "-c"
      - "hba_file=/etc/pg_hba.conf"
    volumes:
      - ./master-postgresql.conf:/tmp/postgresql.conf
      - ./pg_hba.conf:/etc/pg_hba.conf
    healthcheck:
     test: 'pg_isready -U admin --dbname=JobMarketDB'
     interval: 10s
     timeout: 5s
     retries: 5
    networks:
     - network-postgres-test1



  job_market_db_replica:
    image: job_market_database
    container_name: job_market_db_replica
    ports:
      - 5434:5432
    environment:
      PGUSER: replicator
      PGPASSWORD: replicator_password
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: test
    command: |
      bash -c "
      pwd
      until pg_basebackup --pgdata=/var/lib/postgresql/data -R --slot=replication_slot --host=172.24.0.1 --port=5432
      do
      echo 'Waiting for primary to connect...'
      sleep 5s
      done
      echo 'Backup done, starting replica...'
      chmod 0700 /var/lib/postgresql/data
      docker-entrypoint.sh -c config_file=/etc/postgresql/postgresql.conf
      "
    depends_on:
      - job_market_db_master
    volumes:
      - ./postgresql.conf:/etc/postgresql/postgresql.conf
      - ./pg_hba.conf:/etc/pg_hba.conf
    healthcheck:
     test: 'pg_isready -U admin --dbname=JobMarketDB'
     interval: 10s
     timeout: 5s
     retries: 5
    networks:
     - network-postgres-test1

  job_market_notification:
    container_name: job_market_notification
    image: job_market_notification
    ports:
      - "8082:8082"
    healthcheck:
      test: "curl --fail --silent 172.22.0.1:8082/actuator/health | grep UP || echo 'fail'"
      interval: 20s
      timeout: 5s
      retries: 3
    depends_on:
      job_market_db_fill:
        condition: service_completed_successfully
    environment:
      DATASOURCE_URL: jdbc:postgresql://172.22.0.1:5432/JobMarketDB
      DATASOURCE_USERNAME: admin
      DATASOURCE_PASSWORD: test
      SPRING_MAIL_USERNAME: 259905@student.pwr.edu.pl
      SPRING_MAIL_PASSWORD: 'qjuz bobg wbla cryx'
    networks:
      - inner_network

  job_market_user_service:
    container_name: job_market_user_service
    image: job_market_user_service
    ports:
      - "8081:8081"
    healthcheck:
      test: "curl --fail --silent 172.22.0.1:8081/actuator/health | grep UP || echo 'fail'"
      interval: 20s
      timeout: 5s
      retries: 3
    depends_on:
      job_market_db_fill:
        condition: service_completed_successfully
    env_file: .env
    environment:
      DATASOURCE_URL: jdbc:postgresql://172.22.0.1:5432/JobMarketDB
      DATASOURCE_USERNAME: admin
      DATASOURCE_PASSWORD: test
      JWT_SECRET: Fq3b1zVuRCrxOvzIrMp9lGhisD1bCuVuQ6hmudzNnTP1oNnaoe9NBws1Yv70k4tvhrviXs9R9oNyXBB79D1KgEjZIE8zNqJxf4QYHEKAMhsve4KlUtTQt4pCmWuBN1X1
      JWT_EXPIRATIONS_S: 12000
      JWT_REFRESH_EXPIRATIONS_S: 12000

    networks:
      - inner_network

  job_market_db_fill:
    container_name: job_market_db_fill
    image: job_market_db_fill:latest
    depends_on:
      job_market_db:
        condition: service_healthy
    networks:
      - inner_network

  job_market_gateway:
    container_name: job_market_gateway
    image: job_market_gateway
    ports:
      - "8080:8080"
    environment:
      JWT_SECRET: Fq3b1zVuRCrxOvzIrMp9lGhisD1bCuVuQ6hmudzNnTP1oNnaoe9NBws1Yv70k4tvhrviXs9R9oNyXBB79D1KgEjZIE8zNqJxf4QYHEKAMhsve4KlUtTQt4pCmWuBN1X1
    depends_on:
      job_market_db:
        condition: service_healthy
    networks:
      - inner_network

  job_market_chat_service:
    container_name: job_market_chat
    image: job_market_chat
    environment:
      DB_HOST: 172.22.0.1
    depends_on:
      job_market_db_fill:
        condition: service_completed_successfully
    ports:
    - "8088:8088"
    networks:
      - inner_network

  job_market_job_service:
    container_name: job_market_job_service
    image: job_market_job_service
    env_file: .env
    environment:
      DATASOURCE_URL: jdbc:postgresql://172.22.0.1:5432/JobMarketDB
      DATASOURCE_USERNAME: admin
      DATASOURCE_PASSWORD: test
      USER_SERVICE_URL: http://172.22.0.1:8081
    depends_on: 
      job_market_db_fill:
        condition: service_completed_successfully
    ports:
    - "8083:8083"

    networks:
      - inner_network

  rabbitmq:
      image: rabbitmq:3.12-management
      container_name: rabbitmq
      ports:
        - "5672:5672"
        - "15672:15672"
      networks:
        - inner_network

networks:
  inner_network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.22.0.0/16
